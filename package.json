{
  "name": "yachtflow-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently --kill-others \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build:client": "npm run build --prefix client",
    "build": "npm install --prefix client && npm install --prefix server && npm run build:client && npx prisma generate --schema=server/prisma/schema.prisma --config=server/prisma.config.ts",
    "start": "node server/index.js",
    "db:seed": "node server/seed.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
